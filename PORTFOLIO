<!DOCTYPE html>
<html>
<head>
  <title>Graphic Design Projects</title>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="style2.css">
  <link rel="stylesheet" href="styles3.css">
</head>

<body>

<!-- NAVBAR -->
<nav class="navbar">
  <div class="logo">AJ.</div>
  <ul class="nav-links">
    <li><a href="MainPortfolio.html#home">HOME</a></li>
    <li><a href="MainPortfolio.html#about">ABOUT</a></li>
    <li><a href="MainPortfolio.html#experience">EXPERIENCE</a></li>
    <li><a href="MainPortfolio.html#projects">PROJECTS</a></li>
    <li><a href="MainPortfolio.html#contact">CONTACT</a></li>
  </ul>
</nav>

<br><br><br>

<!-- GRAPHIC DESIGN -->
<section class="projects-section">
<div class="container">

<p class="section-tag">PROJECTS</p>
<h2 class="section-title">Graphic Design Work</h2>

<div class="design-gallery">

<div class="design-item">
<img src="FashionShow.jpg" onclick="openModal(this.src)">
<p class="design-title">Fashion Show Poster</p>
</div>

<div class="design-item">
<img src="Christmas.jpg" onclick="openModal(this.src)">
<p class="design-title">Christmas Campaign</p>
</div>

<div class="design-item">
<img src="Drawing.jpg" onclick="openModal(this.src)">
<p class="design-title">Creative Illustration</p>
</div>

<div class="design-item">
<img src="IISPOSTER.png" onclick="openModal(this.src)">
<p class="design-title">College Event Poster</p>
</div>

<div class="design-item">
<img src="igpost.jpg" onclick="openModal(this.src)">
<p class="design-title">Coffee Shop Poster</p>
</div>

<div class="design-item">
<img src="card.png" onclick="openModal(this.src)">
<p class="design-title">Coffee Shop Visiting Card</p>
</div>

</div>

<!-- IMAGE MODAL -->
<div id="imageModal" class="modal">
<span class="close" onclick="closeModal()">&times;</span>
<img class="modal-content" id="modalImg">
</div>

</div>
</section>

<!-- SOFTWARE PROJECTS -->
<section class="projects-section" >
<p class="section-tag">PROJECTS</p>
<h2 class="section-title">Software Projects</h2>

<!-- C PROJECT -->
<div class="project-block">

<h3 class="project-title">Student Management System (C)</h3>

<p class="project-desc">
A console-based application built using C to manage student records 
including adding, updating, deleting, and viewing data using file handling.
</p>

<div class="tech-stack">
<span>C</span>
<span>File Handling</span>
</div>

<img src="output.png" class="C-output">

<p class="hint-text">Click below to explore the implementation</p>

<button class="view-code-btn" id="toggleCodeBtn" onclick="toggleCode()">View Code</button>

<div class="code-container" id="codeBlock">
<pre><code>#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
#include &lt;string.h&gt;

struct Student {
    int id;
    char name[50];
    float marks;
};

void addStudent() {
    FILE *fp = fopen("students.txt", "ab");
    struct Student s;

    printf("Enter ID: ");
    scanf("%d", &s.id);
    printf("Enter Name: ");
    scanf(" %[^\n]", s.name);
    printf("Enter Marks: ");
    scanf("%f", &s.marks);

    fwrite(&s, sizeof(s), 1, fp);
    fclose(fp);

    printf("Student added successfully!\\n");
}

void viewStudents() {
    FILE *fp = fopen("students.txt", "rb");
    struct Student s;

    printf("\\n--- Student List ---\\n");

    while (fread(&s, sizeof(s), 1, fp)) {
        printf("ID: %d | Name: %s | Marks: %.2f\\n", s.id, s.name, s.marks);
    }

    fclose(fp);
}

void searchStudent() {
    FILE *fp = fopen("students.txt", "rb");
    struct Student s;
    int id, found = 0;

    printf("Enter ID to search: ");
    scanf("%d", &id);

    while (fread(&s, sizeof(s), 1, fp)) {
        if (s.id == id) {
            printf("Found: %s | Marks: %.2f\\n", s.name, s.marks);
            found = 1;
        }
    }

    if (!found) printf("Student not found!\\n");

    fclose(fp);
}

void deleteStudent() {
    FILE *fp = fopen("students.txt", "rb");
    FILE *temp = fopen("temp.txt", "wb");

    struct Student s;
    int id;

    printf("Enter ID to delete: ");
    scanf("%d", &id);

    while (fread(&s, sizeof(s), 1, fp)) {
        if (s.id != id) {
            fwrite(&s, sizeof(s), 1, temp);
        }
    }

    fclose(fp);
    fclose(temp);

    remove("students.txt");
    rename("temp.txt", "students.txt");

    printf("Deleted successfully!\\n");
}

int main() {
    int choice;

    do {
        printf("\\n1. Add Student\\n2. View Students\\n3. Search\\n4. Delete\\n5. Exit\\n");
        printf("Enter choice: ");
        scanf("%d", &choice);

        switch (choice) {
            case 1: addStudent(); break;
            case 2: viewStudents(); break;
            case 3: searchStudent(); break;
            case 4: deleteStudent(); break;
            case 5: printf("Exiting...\\n"); break;
            default: printf("Invalid choice!\\n");
        }

    } while (choice != 5);

    return 0;
}
</code></pre>
</div>

</div>

<!-- PYTHON PROJECT -->
<div class="project-block">

<h3 class="project-title">Netflix Data Analytics Dashboard (Python)</h3>

<p class="project-desc">
A data analysis project using Python to explore Netflix content trends including genres, 
country distribution, and content growth over time. Built using Pandas and Matplotlib 
with a Netflix-style visualization theme.
</p>

<div class="tech-stack">
<span>Python</span>
<span>Pandas</span>
<span>Matplotlib</span>
</div>

<p class="hint-text">Click below to explore the implementation</p>

<!-- VIEW OUTPUT BUTTON (NEW) -->
<div class="btn-group">
  <button class="view-code-btn" id="toggleNetflixBtn" onclick="toggleNetflixCode()">View Code</button>
  <button class="view-output-btn" onclick="openGallery()">View Output</button>
</div>
<div class="code-container" id="netflixCodeBlock">
<pre><code>
# Import Libraries
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# -----------------------------
# LOAD DATASET (ROBUST VERSION)
# -----------------------------
df = pd.read_csv("netflix_titles.csv", engine="python")

# Clean column names (IMPORTANT FIX)
df.columns = df.columns.str.strip().str.lower()

print("\nColumns in dataset:")
print(df.columns)

print("\nFirst 5 rows:")
print(df.head())

# -----------------------------
# HANDLE COLUMN NAME ISSUES
# -----------------------------

# Rename columns if needed (safety)
column_map = {
    'listed_in': 'listed_in',
    'type': 'type',
    'country': 'country',
    'date_added': 'date_added',
    'rating': 'rating',
    'duration': 'duration'
}

# Ensure required columns exist
for col in column_map:
    if col not in df.columns:
        print(f" Column '{col}' not found in dataset!")

# -----------------------------
# DATA CLEANING
# -----------------------------

# Use safe fill (won't crash)
if 'country' in df.columns:
    df['country'].fillna('Unknown', inplace=True)

if 'rating' in df.columns:
    df['rating'].fillna('Not Rated', inplace=True)

if 'duration' in df.columns:
    df['duration'].fillna('0', inplace=True)

# Convert date safely
if 'date_added' in df.columns:
    df['date_added'] = pd.to_datetime(df['date_added'], errors='coerce')
    df['year_added'] = df['date_added'].dt.year

# -----------------------------
# GENRE PROCESSING (BEST METHOD)
# -----------------------------
if 'listed_in' in df.columns:
    genre_list = df['listed_in'].str.split(',').explode().str.strip()
else:
    genre_list = pd.Series()

# -----------------------------
# SHOW INSIGHTS FIRST (IMPORTANT)
# -----------------------------
print("\n========== FINAL INSIGHTS ==========")

if not genre_list.empty:
    print(" Most Popular Genre:", genre_list.value_counts().idxmax())

if 'country' in df.columns:
    print(" Top Country:", df['country'].value_counts().idxmax())

if 'year_added' in df.columns:
    print(" Most Active Year:", df['year_added'].value_counts().idxmax())

if 'type' in df.columns:
    print("\n Content Distribution:")
    print(df['type'].value_counts())

# -----------------------------
# GRAPH SETTINGS
# -----------------------------
plt.style.use('dark_background')

# -----------------------------
# 1. GENRE GRAPH
# -----------------------------
if not genre_list.empty:
    plt.figure(figsize=(10,6))
    genre_list.value_counts().head(10).plot(kind='bar')
    plt.title("Top Genres on Netflix")
    plt.xlabel("Genre")
    plt.ylabel("Count")
    plt.xticks(rotation=45)
    plt.tight_layout()
    plt.show()

# -----------------------------
# 2. COUNTRY GRAPH
# -----------------------------
if 'country' in df.columns:
    plt.figure(figsize=(10,6))
    df['country'].value_counts().head(10).plot(kind='bar')
    plt.title("Top Countries Producing Content")
    plt.xlabel("Country")
    plt.ylabel("Count")
    plt.xticks(rotation=45)
    plt.tight_layout()
    plt.show()
# -----------------------------
# 3. TREND GRAPH
# -----------------------------
if 'year_added' in df.columns:
    plt.figure(figsize=(10,6))
    df['year_added'].value_counts().sort_index().plot()
    plt.title("Content Added Over Years")
    plt.xlabel("Year")
    plt.ylabel("Count")
    plt.tight_layout()
    plt.show()

# -----------------------------
# 4. PIE CHART
# -----------------------------
if 'type' in df.columns:
    plt.figure(figsize=(6,6))
    df['type'].value_counts().plot(kind='pie', autopct='%1.1f%%')
    plt.title("Movies vs TV Shows")
    plt.ylabel("")
    plt.tight_layout()
    plt.show()
# -----------------------------
# 5. RATINGS GRAPH
# -----------------------------
if 'rating' in df.columns:
    plt.figure(figsize=(12,8))
    df['rating'].value_counts().head(10).plot(kind='bar')
    plt.title("Top Ratings on Netflix")
    plt.xlabel("Rating")
    plt.ylabel("Count")
    plt.xticks(rotation=45)
    plt.show()

# Keep terminal open
input("\nPress Enter to exit...")
</code></pre>
</div>

</div>

<!-- ✅ GALLERY MODAL (FIXED POSITION) -->
<div id="galleryModal" class="gallery-modal">
<span class="close-btn" onclick="closeGallery()">&times;</span>

<div class="gallery-content">
<img src="OutputPython.png">
<img src="TopCountries.png">
<img src="TopGenere.png">
<img src="TopRatings.png">
<img src="PieChart.png">
<img src="LineChart.png">
</div>
</div>

<!-- SCRIPT -->
<script>

function toggleCode() {
  const code = document.getElementById("codeBlock");
  const btn = document.getElementById("toggleCodeBtn");

  if (code.style.display === "block") {
    code.style.display = "none";
    btn.innerText = "View Code";
  } else {
    code.style.display = "block";
    btn.innerText = "Hide Code";
  }
}

function toggleNetflixCode() {
  const code = document.getElementById("netflixCodeBlock");
  const btn = document.getElementById("toggleNetflixBtn");

  if (code.style.display === "block") {
    code.style.display = "none";
    btn.innerText = "View Code";
  } else {
    code.style.display = "block";
    btn.innerText = "Hide Code";
  }
}

function openModal(src) {
  document.getElementById("imageModal").style.display = "block";
  document.getElementById("modalImg").src = src;
}

function closeModal() {
  document.getElementById("imageModal").style.display = "none";
}

function openGallery() {
  document.getElementById("galleryModal").style.display = "block";
}

function closeGallery() {
  document.getElementById("galleryModal").style.display = "none";
}

window.onclick = function(event) {
  const modal = document.getElementById("galleryModal");
  if (event.target === modal) {
    modal.style.display = "none";
  }
}
</script>
</body>
</html>
/* GRID */
.navbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  width: 100%;
  box-sizing: border-box;   
  padding: 20px 80px;
  position: fixed;
  top: 0;
  left: 0;
  background: #020617;
}

.design-gallery {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 25px;
  margin-top: 30px;
}

/* FIXED IMAGE STYLE */
.design-gallery img {
  width: 100%;            /* full width */
  height: 250px;          /* FIXED HEIGHT */
  object-fit: cover;      /* crop nicely */
  border-radius: 15px;
  border: 1px solid #1e293b;
  cursor: pointer;
  transition: 0.3s;
}

/* CARD */
.design-item {
  text-align: center;
}


/* IMAGE HOVER */
.design-gallery img:hover {
  transform: scale(1.05);
  box-shadow: 0 10px 30px rgba(56, 189, 248, 0.3);
  border-color: #38bdf8;
}

/* TITLE */
.design-title {
  margin-top: 10px;
  font-size: 15px;
  color: #94a3b8;
  transition: 0.3s;
}

.design-item:hover .design-title {
  color: #38bdf8;
  font-weight: 500;
}

/* BACK BUTTON */
.back-btn {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 25px;
  padding: 12px 22px;
  background: transparent;   
  border: 1px solid #1e293b; 
  border-radius: 10px;
  color: #cbd5f5;
  text-decoration: none;
  font-size: 15px;
  box-shadow: 0 6px 20px rgba(56, 189, 248, 0.4);
}

/*  HOVER EFFECT */
.back-btn:hover {
  color: #020617;
  border: 1px solid transparent;
  color: white;
  transform: translateY(-2px);
}
/* MODAL */
.modal {
  display: none;
  position: fixed;
  z-index: 9999;
  padding-top: 80px;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  background: rgba(0,0,0,0.9);
}

.modal-content {
  display: block;
  margin: auto;
  max-width: 80%;
  max-height: 80%;
  border-radius: 10px;
}

.close {
  position: absolute;
  top: 30px;
  right: 50px;
  color: white;
  font-size: 40px;
  cursor: pointer;
}
.section-tag {
  color: #38bdf8;
  font-size: 13px;
  letter-spacing: 2px;
}

.section-title {
  font-size: 45px;   
  font-weight: 600;
}

.projects-section {
  padding: 0 10%;
  margin-top: 80px;
}

.project-block {
  text-align: left !important;
  margin-top: 40px;
  width: 100%;   
  padding: 30px;
  background: #0f172a;
  border-radius: 18px;
  border: 1px solid #38bdf8;
  transition: 0.3s;
}

.project-block:hover {
  transform: translateY(-5px);
  box-shadow: 0 6px 20px rgba(56, 189, 248, 0.4);
}
.project-title {
  font-family: Times New Roman;
  font-size: 35px;
  font-weight: 600;
  margin-bottom: 15px;
  color: #e2e8f0;  
}
.project-block:hover .project-title {
  color: #38bdf8;
}
.project-desc {
  color: #cbd5f5;   
  font-size: 20px;
  line-height: 1.6;
  margin-bottom: 25px;
}
.tech-stack span {
  background: #020617;
  color: #38bdf8;
  border: 1px solid #1e293b;
  padding: 10px 12px;
  border-radius: 10px;
  font-size: 15px;
}

.C-output {
  width: 100%;
  max-width: 500px;
  margin-top: 25px;
  border-radius: 12px;
  border: 1px solid #1e293b;
  display: block;
}

/* BUTTON */
.view-code-btn {
  background: transparent;
  border: 1px solid #38bdf8;
  color: #38bdf8;
}

.view-code-btn:hover {
  background: #38bdf8;
  color: #020617;
}

/* CODE BLOCK */
.code-container {
  display: none;
  margin-top: 20px;
  background: #020617;
  padding: 20px;
  border-radius: 12px;
  overflow-x: auto;
}

code {
  font-family: monospace;
  color: #e2e8f0;
  font-size: 13px;
}

/* HINT TEXT */
.hint-text {
  color: #94a3b8;
  font-size: 18px;
  margin-top: 40px;
  padding-bottom: 5px;
}

/* GRID FOR PROJECT IMAGES */
.p-images {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20px;
  margin-top: 20px;
}

/* IMAGE STYLE */
.p-images img {
  width: 100%;
  border-radius: 12px;
  cursor: pointer;
  transition: 0.3s;
}

/* HOVER EFFECT */
.p-images img:hover {
  transform: scale(1.05);
  box-shadow: 0 0 20px rgba(229, 9, 20, 0.4); /* Netflix glow */
}
/* MODAL BACKGROUND */
.gallery-modal {
  display: none;
  position: fixed;
  z-index: 999;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  background: rgba(0,0,0,0.9);
}

/* CONTENT AREA */
.gallery-content {
  margin: 60px auto;
  width: 80%;
  max-height: 80vh;
  overflow-y: scroll;

  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20px;
}

/* IMAGES */
.gallery-content img {
  width: 100%;
  border-radius: 10px;
  transition: 0.3s;
}

.gallery-content img:hover {
  transform: scale(1.05);
}

/* CLOSE BUTTON */
.close-btn {
  position: absolute;
  top: 20px;
  right: 40px;
  font-size: 40px;
  color: white;
  cursor: pointer;
}

/* BUTTON STYLE */
.view-output-btn {
  background-color: transparent;
  border: 1px solid #e50914;
  color: pink;
}

.view-output-btn:hover {
  background: #e50914;
  color: white;
  transform: translateY(-2px);
}
.btn-group {
  display: flex;
  gap: 15px;  text-align: left !important;
  margin-top: 15px;
  justify-content: flex-start !important;  
}

.view-code-btn,
.view-output-btn {
  height: 42px;
  padding: 0 18px;  
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border-radius: 12px;
  font-size: 16px;
  cursor: pointer;
  transition: all 0.3s ease;
  line-height: 1;  
}
#netflixCodeBlock,
#netflixCodeBlock * {
  text-align: left !important;
}

body{
  background-color: #FFDFD3;
  padding: 25px;
  margin: 0px;
}
.title {
	color: #5C6AC4;
}
.img{
  border: 3px solid #811331;
  border-radius: 20px;
  padding: 10px;
  margin-left: 90px;
  overflow: hidden;
}
nav{
  background-color: #FFFDD0; 
  padding-bottom: 20px;
  padding-top: 10px;
  border: 5px solid #811331; 
  border-radius: 10px; 
  height: 40px;
}
.nav-item{
  position: relative;
  display: inline-block;
  font-size: 30px;
  color: purple;
  padding: 10px 20px;
  margin: 0 20px;
  text-decoration: none;
}
.nav-item .icon{
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  width: 35px;
  opacity: 0;
  transition: 0.3s;
}
.nav-item:hover .text{
  opacity: 0;
}

.nav-item:hover .icon{
  opacity: 1;
}
a{
  font-size: 40px;
  color: purple; 
  padding: 10px; 
  margin: auto 10px ;
}
.layout {
  display: flex;
  gap: 40px;
  align-items: flex-start;
  justify-content: center;
}
.sections-container {
  display: flex;
  flex-direction: column;
  gap: 25px;
}
.container{
  background-color: #C1E1C1; 
  border: 3px solid white;
  width: 1250px;
  height: 500px;
  overflow: hidden; 
  margin-left: 0px; 
  display: flex;
  align-items: center;
  gap: 40px;
  padding: 30px;
  border-radius: 10px;
  position: relative;
}
.intro{
  padding: 20px;
  border-radius: 10px;
}
.introtext{
  font-family: Lucida Calligraphy;
  font-size: 72px;
  color: White; 
  padding: 10px; 
  margin: 20px ;
  margin-left: 0px;
}
.introtext:hover{
  color: #811331;
  letter-spacing: 2px;
  transition: 0.3s;
}

.abouttext{
  font-family: Palatino Linotype;
  font-size: 23px;
  color: #097969; 
  padding: 10px; 
  margin: 20px;
  margin-left: 0px;
  margin-right: 100px;
}
.sticker{
  position: absolute; 
  opacity: 0.9;
  width: 250px;
}
.d1{
  position: absolute;
  right: 10px;
  top: -10px;
  width: 220px;
  animation: popout 2s infinite; 
}
@keyframes popout{
0%{
    transform: scale(1);
  }

  50%{
    transform: scale(1.4);
  }

  100%{
    transform: scale(1);
  }
}
.d2{
  left: -50px;
  width: 150px;
}
.d4{
  bottom: -20px;
  right: -10px;
  width: 220px;
  transform-origin: center;
  animation: popout 2s infinite; 
}
@keyframes popout{
0%{
    transform: scale(1);
  }

  50%{
    transform: scale(1.4);
  }

  100%{
    transform: scale(1);
  }
}

.d5{
  bottom: 200px;
  right: -14px;
  width: 170px;
  animation: moveimage 3s infinite ;
}
@keyframes moveimage{
  0%{
    transform: rotate(0deg) scale(1);
  }

  50%{
    transform: rotate(-40deg) scale(1.1);
  }

  100%{
    transform: rotate(0deg) scale(1);
  }
}
.section1{
  right: 0px;
  top: 0px;
  max-width: 400px;
  background-color: #E37383;
  border-radius: 10px;
  border: 5px solid #811331;
  transition: 0.3s;
}

.section1:hover {
  transform: scale(1.05);
}
.section2{
  max-width: 400px;
  background-color: #E37383;
  border-radius: 10px;
  border: 5px solid #811331;
  transition: 0.3s;
}

.section2:hover {
  transform: scale(1.05);
}
.section3{
  max-width: 400px;
  background-color: #E37383;
  border-radius: 10px;
  border: 5px solid #811331;
  transition: 0.3s;
}

.section3:hover {
  transform: scale(1.05);
}

.qualification-header {
  font-family: Lucida Handwriting;
  padding: 10px;
  cursor: pointer;
  text-align: center;
  color: white;
  font-size: 24px;
  font-weight: bold;
}
.qualification-content {
  font-family: Bell MT;
  margin-top: 0px;
  margin-left: 20px;
  font-size: 30px;
  color: white;
  background-color: #E37383;
  max-height: 0;
  overflow: hidden;
  transition: 0.5s;
}
.skills-content {
  font-family: Bell MT;
  font-size: 25px;
  color: white;
  background-color: #E37383;
  max-height: 0;
  overflow: hidden;
  transition: 0.5s;
}
.qualification-content.show {
  max-height: 1000px;
  padding: 10px;
}
.skills-content.show {
  max-height: 1000px;
  padding: 10px;
}
.qualification-content ul {
  font-size: 22px;
  color: white;
  padding: 10px;
}
.sections-container {
  display: flex;
  gap: 50px;
}
ul{
  margin: 0;
}
.certificate-content {
  font-family: Bell MT;
  margin-top: 0px;
  margin-left: 20px;
  font-size: 23px;
  color: white;
  background-color: #E37383;
  max-height: 0;
  overflow: hidden;
  transition: 0.5s;
}
.certificate-content.show {
  max-height: 450px;
  padding: 10px;
}
.cert-card {
  color: white;              
  text-decoration: none;  
  font-size: 26px;      
  padding: 0px;
  margin: auto 0px;
}
.cert-card {
  display: inline-block;
  transition: 0.3s;
}
.cert-card:hover {
  color: #630330;
  transform: translateX(5px); /* slight move effect */
}  
body {
  margin: 0;
  padding-top: 100px;
  padding: 60px 80px;
  font-family: Bell MT;
  background: radial-gradient(circle at top, rgba(30, 58, 138, 0.25), #000000 70%);
  background-color: #000000;
}
.container {
  max-width: 1200px;
  margin: auto;
  padding: 0 20px;
}
.project-card,
.project-block,
.design-item,
.stat-box {
  position: relative;
  z-index: 1;
}
.myimage:hover {
  transform: translateY(-5px);
  box-shadow: 0 10px 30px rgba(56, 189, 248, 0.5);
}
.navbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  width: 100%;
  box-sizing: border-box;   
  padding: 20px 80px;
  position: fixed;
  z-index: 9999; 
  top: 0;
  left: 0;
  background: #020617;
}
.logo {
  color: white;
  font-weight: 600;
  font-size: 18px;
}
.nav-links {
  list-style: none;
  display: flex;
  gap: 40px;
}

.nav-links a {
  text-decoration: none;
  color: #94a3b8;
  font-size: 14px;
  letter-spacing: 1px;
  transition: 0.3s;
}

/* Hover effect */
.nav-links a:hover {
  color: #38bdf8;
}

/* container */
.flex-1 {
  max-width: 1200px;
  margin: auto;
  padding-top: 30px;
}

/* small heading */
.portfolio {
  font-size: 15px;
  letter-spacing: 3px;
  color: #64748b;
  margin-bottom: 16px;
}

/* main heading */
h1 {
  font-family: Poppins;
  font-size: 75px;
  font-weight: 600;
  color: #e2e8f0;
  margin-bottom: 20px;
}

/* name highlight */
.highlight {
  color: #38bdf8;
}

/* description text */
.description {
  color: #94a3b8;
  font-size: 20px;
  line-height: 1.8;
  margin-bottom: 30px;
}

/* buttons row */
.buttons {
  display: flex;
  gap: 20px;
  align-items: center;
}
.btn-primary,
.btn-secondary,
.contact-btn {
  padding: 14px 28px;
  height: 25px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

/* primary button */
.btn-primary {
  display: inline-flex;      
  align-items: center;
  gap: 8px;     
  background: linear-gradient(90deg, #22d3ee, #38bdf8);
  color: darkblue;
  border: none;
  padding: 12px 22px;
  border-radius: 10px;
  font-size: 16px;
  font-weight: 500;
  cursor: pointer;
  transition: 0.3s ease;
  text-decoration: none;
}
.btn-primary img {
  width: 18px;
  height: 18px;
}
/* hover effect */
.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(56, 189, 248, 0.4);
}

/* secondary button */
.btn-secondary {
  background: #0f172a;
  color: #cbd5f5;
  border: 1px solid #1e293b;
  padding: 12px 22px;
  border-radius: 10px;
  font-size: 16px;
  cursor: pointer;
  text-decoration: none;
}

/* learn more link */
.learn-more {
  color: #94a3b8;
  text-decoration: none;
  font-size: 14px;
  margin-left: 5px;
}

.learn-more:hover {
  color: #38bdf8;
}
.myimage {
  float: right;
  margin-left: 40px;
  width: 270px;
  height: 270px;
  object-fit: cover;
  padding: 10px;
  border: 5px solid lightblue;
  border-radius: 15px;
  transition: 0.3s ease; 
}
.myimage:hover {
  transform: translateY(-5px);
  box-shadow: 0 10px 30px rgba(56, 189, 248, 0.5);
}
/* Section spacing */
.about-section {
  padding: 80px 0px;
  color: white;
}

/* Small heading */
.section-tag {
  font-size: 15px;
  letter-spacing: 3px;
  color: #64748b;
  margin-bottom: 5px;
}

/* Main title */
.section-title {
  font-family: 'Poppins';
  font-size: 55px;
  margin-bottom: 40px;
}

/* Layout */
.about-container {
  display: flex;
  gap: 40px;
}

/* Left big card */
.about-card {
  flex: 1;
  background: rgba(15, 23, 42, 0.6);
  padding: 25px;
  border-radius: 15px;
  border: 1px solid #1e293b;
  color: #94a3b8;
  line-height: 1.6;
}

/* Info section */
.about-info {
  margin-top: 20px;
}
.about-info p {
  margin: 8px 0;
}

/* Right side */
.about-stats {
  flex: 0.8;
  display: flex;
  flex-direction: column;
  gap: 24px;
}
.about-card p {
  font-family: Bell MT;
  font-size: 25px;
  line-height: 1.5;
}
.stat-box p {
  font-family: Bell MT;
  color: #94a3b8;
  font-size: 23px;
}
.about-info p {
  font-family: Bell MT;
  margin: 8px 0;
  font-size: 25px;
}
/* Numbers */
.stat-box h3 {
  color: #38bdf8;
  font-size: 25px;
}
/* Small stat boxes */
.stat-box {
  background: rgba(15, 23, 42, 0.6);
  padding: 15px;
  border-radius: 15px;
  border: 1px solid #1e293b;
  display: flex;
  align-items: center;
  gap: 10px;
  transition: 0.3s ease; /* important */
}

/*  Hover effect */
.stat-box:hover {
  transform: translateY(-5px);
  box-shadow: 0 10px 30px rgba(56, 189, 248, 0.2);
  border-color: #38bdf8;
}

/* Contact section */
.contact-section {
  padding: 100px 0;
}

/* Box styling */
.contact-box {
  text-align: center;
  background: rgba(15, 23, 42, 0.6);
  padding: 50px;
  border-radius: 20px;
  border: 1px solid #1e293b;
}

/* Title */
.contact-title {
  font-size: 48px;
  color: #e2e8f0;
  margin-bottom: 30px;
  margin-top: 20px;
}

/* Description */
.contact-desc {
  color: #94a3b8;
  font-size: 18px;
  margin-bottom: 30px;
}

/* Buttons container */
.contact-buttons {
  display: flex;
  justify-content: center;
  gap: 15px;
  flex-wrap: wrap;
}

/* Buttons */
.contact-btn {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding: 12px 18px;
  border-radius: 10px;
  text-decoration: none;
  font-size: 14px;
  background: #0f172a;
  color: #cbd5f5;
  border: 1px solid #1e293b;
  transition: 0.3s;
}

/* Primary button (email) */
.contact-btn.primary {
  background: linear-gradient(90deg, #22d3ee, #38bdf8);
  color: #020617;
}

/* Icons */
.contact-btn img {
  width: 18px;
  height: 18px;
}

/* Hover effect */
.contact-btn:hover {
  transform: translateY(-2px);
}
.footer {
  text-align: center;
  padding: 20px 0;
  color: #94a3b8;
  font-size: 14px;
  border-top: 1px solid #1e293b;
  margin-top: 40px;
}
/* EXPERIENCE SECTION */
.experience-section {
  padding: 80px 0;
  color: white;
}

/* CARD */
.experience-card {
  background: rgba(15, 23, 42, 0.6);
  padding: 30px;
  border-radius: 20px;
  border: 1px solid #1e293b;
  margin-bottom: 30px;
  transition: 0.3s;
}

.experience-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 10px 30px rgba(56, 189, 248, 0.2);
}

/* HEADER */
.exp-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.exp-header h3 {
  font-size: 26px;
  color: #e2e8f0;
}

.exp-header span {
  font-size: 18px;
  color: #64748b;
}

/* SUBTITLE */
.exp-sub {
  color: #38bdf8;
  margin: 8px 0;
  font-size: 18px;
}

/* DESCRIPTION */
.exp-desc {
  color: #94a3b8;
  font-size: 19px;
  line-height: 1.7;
  margin-top: 10px;
}

/* CERTIFICATE */
.certificate {
  margin-top: 20px;
}

/* SKILLS */
.skills {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  margin-top: 10px;
}

.section-tag {
  padding: 50px 0;
  font-size: 15px;
  letter-spacing: 4px;
  color: #64748b;
}
.skills span {
  background: #0f172a;
  border: 2px solid #1e293b;
  padding: 12px 14px;
  border-radius: 20px;
  font-size: 14px;
  color: #cbd5f5;
  transition: 0.3s;
}

.skills span:hover {
  background: #38bdf8;
  color: black;
  cursor: pointer;
}
/* PROJECTS SECTION */
.projects-section {
  padding: 80px 0;
  color: white;
}

/* Container grid */
.projects-container {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 25px;
}

/* Card */
.project-card {
  background: rgba(15, 23, 42, 0.6);
  padding: 25px;
  border-radius: 15px;
  border: 2px solid #1e293b;
  transition: 0.3s;
}

/* Hover */
.project-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 10px 30px rgba(56, 189, 248, 0.2);
  border-color: #38bdf8;
}

/* Title */
.project-card h3 {
  color: #e2e8f0;
  margin-bottom: 10px;
}

/* Description */
.project-desc {
  color: #94a3b8;
  font-size: 15px;
  margin-bottom: 15px;
}
.cert-btn img {
  width: 18px;
  height: 18px;
  filter: brightness(0) invert(1);
  gap: 50px;
}

.cert-btn:hover img {
  filter: brightness(0) invert(1) drop-shadow(0 0 5px #38bdf8);
}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Apara Jain Portfolio</title>

  <link rel="stylesheet" href="style2.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
</head>

<body>

<!-- NAVBAR -->
<nav class="navbar">
  <div class="logo">AJ.</div>
  <ul class="nav-links">
    <li><a href="#home">HOME</a></li>
    <li><a href="#about">ABOUT</a></li>
    <li><a href="#experience">EXPERIENCE</a></li>
    <li><a href="#projects">PROJECTS</a></li>
    <li><a href="#contact">CONTACT</a></li>
  </ul>
</nav>

<br><br><br>

<!-- HERO -->
<div id="home" class="flex-1 text-left container">
  <p class="portfolio">PORTFOLIO</p>

  <img src="IMG.jpg" width="270" height="270" class="myimage"/>

  <h1>
    Hi, I'm <span class="highlight">Apara Jain</span>
  </h1>

  <p class="description">
I'm a passionate BCA student with a strong interest in web development and programming, designing user-friendly websites and continuously expanding my technical skills. I enjoy learning technologies like HTML, CSS, Java, Python and database management.
  </p>

  <div class="buttons">
    <a href="#contact" class="btn-primary">
      Get in Touch <img src="email.svg" width="20">
    </a>

    <a href="Apara_Jain_Resume2.pdf" class="btn-secondary" target="_blank">
      Download CV
    </a>

    <a href="#about" class="learn-more">Learn more ↓</a>
  </div>
</div>

<!-- ABOUT -->
<section class="about-section" id="about">
  <div class="container">
    <p class="section-tag">ABOUT</p>
    <h2 class="section-title">Background</h2>

    <div class="about-container">
      <div class="about-card">
       <p> My academic journey reflects a strong foundation in both discipline and curiosity. I completed my schooling at Subodh Public School, securing 92% in Class 10 and 95.4% in Class 12 (Commerce). Currently, I am pursuing a Bachelor of Computer Applications (BCA) from IIS (Deemed to be University), and to complement my degree, I am also pursuing a Data Analytics Diploma with a specialization in Python, continuously expanding my skills at the intersection of data and innovation. </p> <div class="about-info"> <p>📍 India</p> <p>💼 Data Analyst | Aspiring Java Student | Graphic Designer </p> <p>🎓 BCA (2024 – Present)</p>
        </div>
      </div>

      <div class="about-stats">
        <div class="stat-box"><h3>2+</h3><p>Years Learning</p></div>
        <div class="stat-box"><h3>3+</h3><p>Projects</p></div>
        <div class="stat-box"><h3>∞</h3><p>Growth</p></div>
        <div class="stat-box"><h3>1+</h3><p>Internship</p></div>
      </div>
    </div>
  </div>
</section>

<!-- EXPERIENCE -->
<section class="experience-section" id="experience"> 
<div class="container"> 
<p class="section-tag">EXPERIENCE</p> 
<h2 class="section-title">Career Timeline</h2>
<div class="experience-card"> 
<div class="exp-header"> 
<h3>SEO Intern</h3> 
<span>Mobifly • 2026</span> 
</div> 
<p class="exp-sub">Internship</p> 
<p class="exp-desc"> 
Completed a one-month internship at Mobifly as an SEO Intern, where I worked on optimizing website content, improving search engine rankings, and analyzing website performance. I gained practical exposure to SEO tools and digital marketing strategies while working on real-world projects. 
</p> 
<p class="exp-desc"> 
Alongside my internship, I have also worked on graphic design projects, creating visually appealing designs and UI elements, which enhanced my creativity and user experience understanding. 
</p> 
<!-- CERTIFICATE BUTTON --> 
<div class="certificate"> 
<a href="C2.pdf" target="_blank" class="btn-secondary cert-btn"> 
<img src="Cicon-removebg-preview.png" alt="certificate"> Certificate </a> 
</div> 
</div>
    <p class="section-tag">SKILLS</p>
    <div class="skills">
<span>HTML</span> 
<span>CSS</span> 
<span>Java (Basics)</span> 
<span>Python</span> 
<span>Database Management</span> 
<span>SEO Optimization</span> 
<span>Keyword Research</span> 
<span>Content Strategy</span> 
<span>Analytics</span> 
<span>Graphic Design</span> 
<span>UI Design</span> 
<span>Canva</span> 
<span>Photoshop (Basic)</span> 
<span>Problem Solving</span> 
<span>Creativity</span> 
<span>Communication</span>
    </div>

  </div>
</section>

<!-- PROJECTS -->
<section class="projects-section" id="projects"> 
<div class="container"> 
<p class="section-tag">
PROJECTS
</p> 
<h2 class="section-title">
My Work
</h2> 
<div class="projects-container"> 
<!-- PROJECT 1 (Graphic Design) --> 
<div class="project-card"> 
<h3>Graphic Design Portfolio</h3> 
<p class="project-desc"> 
A collection of my graphic design work including social creatives and posters, created using Photoshop. Focused on creativity and layout. 
</p> 
<a href="design.html" class="btn-secondary">
View Designs
</a> 
</div> 
<!-- PROJECT 2 --> 
<div class="project-card"> 
<h3>Student M. System (C)</h3> 
<p class="project-desc"> 
A console-based application built using C to manage student records including adding, updating, deleting, and viewing student data. 
</p> 
<a href="design.html#student-project" class="btn-secondary">
View Code
</a> 
</div>  
<!-- PROJECT 3 --> 
<div class="project-card"> 
<h3>Data Analysis with Python</h3> 
<p class="project-desc"> 
Performed data analysis using Python (Pandas, Matplotlib) to clean, visualize, and interpret datasets for meaningful insights. </p> 
<a href="design.html" class="btn-secondary">
View Code
</a> 
</div> 
</div> 
</div> 
</section>
<!-- CONTACT -->
<section class="contact-section" id="contact"> 
<div class="container contact-box" > 
<div class="container"> 
<p class="section-tag">
CONTACT
</p> 
<h2 class="contact-title">
Let's Connect
</h2> 
<p class="contact-desc"> 
I'm always open to discussing new opportunities, creative ideas, or collaborations. 
</p> 
<div class="contact-buttons"> 
<!-- Email --> 
<a href="mailto:apara27.jain@gmail.com" class="contact-btn primary"> 
<img src="email.svg" alt="email"> apara27.jain@gmail.com </a> <!-- LinkedIn --> 
<a href="https://www.linkedin.com/in/apara-jain-129b5331b/" target= "_blank" class="contact-btn"> 
<img src="linkedin.svg" alt="linkedin"> LinkedIn </a> 
<!-- Phone --> 
<a href="tel:+9199290 882149" class="contact-btn"> 
<img src="phone.svg" alt="phone"> +91 99290 88149 </a>
 <!-- Instagram --> <ahref="https://www.instagram.com/j.aparaaaaa" target="_blank" " class="contact-btn"> 
<img src="instagram.svg" alt="instagram"> Instagram </a> </div> 
</div> 
</section>
<footer class="footer"> 
<p>© 2026 Apara Jain. All rights reserved.</p> 
</footer> 
</body> 
</html>

</body>
</html>
